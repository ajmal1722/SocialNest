import React from 'react'

const ScrollableScreen = ({ content }) => {
    return (
        <div className='w-full lg:col-span-6'>
            {content}
        </div>
    )
}

export default ScrollableScreen
